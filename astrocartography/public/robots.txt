# https://natalnavigator.com robots.txt
User-agent: *
Allow: /
Disallow: /api/

# Private routes are handled via X-Robots-Tag: noindex headers (see vercel.json)
# so Googlebot can fetch + process the noindex directive instead of being blocked here.

# Sitemap
Sitemap: https://natalnavigator.com/sitemap.xml
